 "use client"; import {useState} from "react"; import Link from "next/link"; export default function Register(){const[f,setF]=useState({name:"",email:"",phone:"",password:""});const[m,setM]=useState("");return <main style={{padding:40,fontFamily:"Arial"}}><h1>Daftar Customer</h1><form onSubmit={async e=>{e.preventDefault();const r=await fetch("/api/auth/register",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(f)});const d=await r.json();if(r.ok)location.href="/login";else setM(d.error)}} style={{display:"grid",gap:10,maxWidth:420}}>{["name","email","phone","password"].map(k=><input key={k} type={k==="password"?"password":"text"} placeholder={k} value={(f as any)[k]} onChange={e=>setF({...f,[k]:e.target.value})}/>) }<button>Daftar</button></form><p>{m}</p><Link href="/login">Login</Link></main>}